1. Need to make sure storage class is setup for defaults and have clear docs on what to look for if nim-operator fails to deploy due to storageclass issues
2. Make a script that checks for storage classes. Can this be done with a post-rendered script?
